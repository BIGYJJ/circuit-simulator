# 画布编辑交互契约

本增量把画布编辑定义为一系列确定性的文档命令，而不是一组直接修改 SVG 的视觉效果。画布仅负责将指针坐标转换到固定的电路坐标系、显示拖拽或连线预览，并在用户完成一次意图明确的操作时向上层提交文档变更。这样可以保证保存、撤销、重做、结构校验与未来协作使用同一份电路事实。

| 用户动作 | 临时视觉状态 | 提交的文档结果 | 失败或取消行为 |
| --- | --- | --- | --- |
| 从元件库拖至网格 | 组件卡片进入拖放态，画布提示落点 | 在吸附到 25 单位网格的坐标新增元件 | 落在画布外则不新增 |
| 拖动已有元件 | 元件和相邻导线跟随指针移动 | 更新该元件的 `x`、`y` 坐标 | 指针取消则保留原坐标 |
| 从端口拖向另一端口 | 出现一条萤光石灰预览线；最近有效端口高亮 | 新增一条端点唯一的 `CircuitWire` | 松开在无效区域、同一端口或已有同向连接上则取消 |
| 点击已有导线 | 导线显示选中态 | 不改变电路文档 | 未选中导线不响应删除 |
| 删除选中导线 | 选中态短暂淡出 | 从 `wires` 移除对应导线 | 没有选中导线则无操作 |
| 撤销或重做 | 画布按恢复的文档重绘 | 在过去栈和未来栈之间移动完整文档快照 | 边界栈为空时禁用对应按钮 |

## 坐标与吸附

画布逻辑坐标固定为 `1000 × 650`，而 CSS 缩放只影响显示，不改变保存的电路坐标。每次落点和移动完成时都被量化到 25 单位网格。端口是由元件种类和元件逻辑坐标派生的只读点：电阻与电压源有 `top` 和 `bottom`，参考地只有 `top`。端口吸附半径为 24 逻辑单位，在半径内优先选择距离最近且不是起始端口的候选端口。

## 命令与历史

每一个完成的新增元件、移动元件、新增导线、删除导线和参数应用都会创建一次文档快照命令。正在拖动、正在连线和正在输入的草稿都不进入历史记录；只有指针松开到有效目标或点击“应用参数”才提交。一次新编辑会清空重做栈，历史保留最近 20 个完整文档快照。每个文档变更都会使旧仿真快照失效，直到用户再次点击“运行仿真”。

## 结构与求解边界

编辑器允许临时存在未接线的元件和不完整电路，因为学习者需要能观察错误状态。`validateDocument` 继续负责基础结构合法性；分压求解器继续负责识别“不是可求解的完整分压支路”。这两个层级的诊断必须在界面中明确区分：前者表示文档无效，后者表示文档可保存但当前无法进行该项线性 DC 分析。
