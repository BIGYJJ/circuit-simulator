# 首期架构决策：可视化电路模拟器

> 状态：已采纳。范围：第一个可验证的线性 DC 分压器纵向切片。

本工程以本地优先的浏览器应用开始。第一版不要求用户账号，也不把电路项目或仿真数据发送至服务器；项目文档保存在浏览器本地，计算仅使用前端 TypeScript 模块。该边界使学习者能够即时实验，并避免在核心模型尚不稳定时引入服务端状态与协作冲突。

| 决策 | 实施方式 | 原因 |
| --- | --- | --- |
| 电路事实与图形分离 | `circuit-model.ts` 只保存元件、端口、导线、参数与格式版本 | 移动元件不应改变拓扑，未来可独立导入、导出或网表编译 |
| 画布与求解分离 | 画布仅将文档投影为符号与连线；求解器只接收电路文档 | 避免 React 视图层污染数值计算，便于移入 Worker 或 WASM |
| 小范围先行 | 仅支持一个可连接的电压源—两电阻—地分压拓扑 | 先建立正确性基线，再扩展 RC、二极管和数字逻辑 |
| 可恢复的本地保存 | 文档带 `version` 并在读取时校验 | 早期就形成兼容与迁移意识，降低后续格式演进风险 |
| 质量门禁 | 类型检查、单元测试和生产构建均作为通过条件 | 将电路正确性从视觉直觉转为可重复的工程证据 |

首期模块位于 `client/src/lib`，避免过早引入复杂单体仓库工具。模块数量和边界在需求真实增长后再提升为 `packages/*`；在此之前，保持代码可读、测试可跑比目录形式更重要。

## 验收边界

一条功能只有在下列条件同时满足时才可被标为首期完成：其编辑行为可以恢复、文档可以保存、结构错误有明确诊断、求解结果可由测试验证，且界面不将仿真描述为真实硬件安全认证。非线性器件、SPICE 导入、多人协作和硬件串口均明确不属于本切片。
