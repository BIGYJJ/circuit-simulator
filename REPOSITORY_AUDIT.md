# 仓库审计记录：BIGYJJ/circuit-simulator

> 审计日期：2026-08-27。审计目的：决定现有仓库应被复用、渐进重构，还是仅保留为项目占位并在新工程中实现首个可验证纵向切片。

## 结论

用户选定的 [BIGYJJ/circuit-simulator][1] 当前为公开仓库，但克隆结果为空，且仓库元数据没有默认分支、描述或许可证信息。因此没有可安全复用的源代码、测试、第三方依赖、设计资产或授权条款可供审计。首期将以本工程的 React + TypeScript 静态应用脚手架为实施起点，并将外部仓库保留为未来同步目标，而不是迁移源。

## 观察记录

| 审计维度 | 观察结果 | 对首期的影响 |
| --- | --- | --- |
| 可见性 | 仓库为 Public | 可作为未来公开项目的远端目标，但当前不包含可评估实现 |
| Git 内容 | 克隆工具明确报告为空仓库 | 无需迁移历史、文件或依赖；不能以其作为技术基线 |
| 默认分支 | 未设置 | 在同步前必须先创建可审阅的初始提交与默认分支 |
| 许可证 | 未设置 | 对外发布、复用第三方贡献和协作前必须选择并添加许可证 |
| 描述与文档 | 未提供 | 必须由本项目补齐目标、架构、运行方式、限制与贡献规范 |
| 自动化质量门禁 | 不存在 | 在第一个功能合并前建立类型检查、单元测试和构建验证 |

## 架构决策

**ADR-001：新建而非迁移。** 首个实现将放在 `circuit-simulator-web`，采用本地优先的浏览器应用架构。编辑模型、网表编译和直流求解器将被编写为尽量独立的 TypeScript 模块，界面只负责工具、画布和读数展示。该选择与已批准计划中的“画布和求解器彻底解耦”一致，并避免针对不存在的历史代码做无依据的兼容设计。

**ADR-002：首个范围限定为线性 DC 分压器。** 初版将包含电压源、地、两个电阻、端口连线、节点电压读数、保存/恢复和明确的未接地诊断。暂不实现 SPICE 兼容、非线性器件、瞬态分析、数字逻辑、云端项目或多人协作。只有该切片具备可重复的正确性测试后，才会扩展 RC 瞬态和更复杂元件。

**ADR-003：外部仓库在首个可用版本后再同步。** 本工程通过类型检查、核心求解器测试、构建及用户可操作验证后，再以干净的初始提交同步到用户选定仓库。同步前应补齐 README、许可证、贡献规范和安全说明，并由用户在发布层面决定可见性与许可策略。

## 首期成功标准

| 编号 | 可验证结果 |
| --- | --- |
| S1 | 用户可在深色无限画布中放置电源、电阻、地并通过端口连线构成分压器。 |
| S2 | 改变电源与电阻参数后，节点电压读数根据线性 DC 方程重新计算。 |
| S3 | 缺失地、未连接端口或不完整分压网络会显示可定位、可理解的诊断信息。 |
| S4 | 电路文档以版本化 JSON 保存到本地，并能在刷新后恢复。 |
| S5 | 核心求解逻辑具有覆盖典型分压、边界值和错误分支的自动化测试。 |

## 下一步

下一步将建立这些成功标准所需的目录边界、核心领域类型、测试夹具与质量脚本，然后实现画布编辑器和可解释的分压器求解纵向切片。该实施不会将仿真结果表述为实际硬件安全认证；界面将持续呈现模型假设和教学性限制。

## 参考资料

[1]: https://github.com/BIGYJJ/circuit-simulator
