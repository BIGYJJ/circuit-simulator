# 可视化电路模拟器：技术评审说明

## 评审版本

本归档对应 GitHub `main` 分支提交 **`9f17dcf`**，以及可恢复项目版本 **`manus-webdev://9f17dcf9`**。该版本在本地静态前端工程中实现了从基础电路学习实验到高级工程运营控制台的一组连续能力。评审时应将所有数值输出理解为代码中显式定义的**教学模型**，而不是实际器件认证、完整 SPICE 结果或硬件安全结论。

| 项目 | 内容 |
| --- | --- |
| 前端框架 | React 19、TypeScript、Vite、Tailwind CSS 4 |
| 本地运行 | `pnpm install` 后执行 `pnpm dev` |
| 类型验证 | `pnpm check` |
| 自动化验证 | `pnpm test`，当前基线为 8 个测试文件、31 个测试用例 |
| 生产构建 | `pnpm build` |
| 远程代码 | [BIGYJJ/circuit-simulator](https://github.com/BIGYJJ/circuit-simulator) 的 `main` 分支 |

## 可评审能力

系统以深色精密实验工作台为统一界面语言，当前包含四个主入口。`/` 是理想 RC 充电、保持和放电实验，涵盖可编辑画布、时间游标、示波器、能量与功耗证据。`/led` 是 LED 工作点与故障挑战，使用常压降教学近似。`/divider` 是自由搭建的线性 DC 分压器。`/engineering` 与 `/engineering/ops` 组织从非线性分析到工程运营的高级学习模块。

| 入口 | 主要能力 | 核心评审问题 |
| --- | --- | --- |
| `/` | RC 瞬态、拖拽与连线、时间显微镜、CSV/JSON 导出 | 时间样本、工作台状态和模型边界是否一致、清晰 |
| `/led` | LED 常压降工作点、探针、开路/短路/错误阻值、学习挑战 | 故障证据是否与工作点数据一致，安全表述是否克制 |
| `/divider` | 线性分压求解、端口吸附、撤销重做、结构诊断 | 编辑文档与求解拓扑之间的分离是否可靠 |
| `/engineering` | 二极管迭代/扫描、BJT、MOSFET、RC 数值、AC/Bode、数字/混合信号、SPICE 子集、测试台 | 每种分析是否明确模型、输入、输出和限制 |
| `/engineering/ops` | 容差角点、触发游标、约束、变更影响、门禁、备份、风险和交付摘要 | “工程证据”是否可追溯，教学假设是否没有被包装成认证结论 |

## 架构概览

系统的关键设计选择是将**电路事实、求解/分析、可视化与导出**分离。版本化电路文档不依赖 SVG 坐标表现；求解器不读取页面状态；画布、仪器与检查器仅投影确定性结果。这使得后续迁移到 Web Worker、WebAssembly 或更完整的 MNA/SPICE 内核时，可以保留项目格式和学习交互。

| 层次 | 关键位置 | 责任 |
| --- | --- | --- |
| 领域模型 | `client/src/lib/circuit-model.ts` | 元件、端口、导线、版本格式、结构校验、本地恢复 |
| 基础求解 | `circuit-solver.ts`、`rc-charge-solver.ts`、`led-solver.ts` | 分压、理想 RC、LED 教学工作点与诊断 |
| 高级分析 | `engineering-core.ts` | 迭代、扫描、BJT/MOSFET、数值近似、AC、信号、逻辑、ERC、SPICE 子集、断言 |
| 工程运营 | `engineering-ops.ts` | 容差角点、触发、约束、变更影响、风险、门禁、备份格式 |
| 可视化工作台 | `client/src/pages/`、`client/src/components/` | 实验画布、仪器读数、学习解释、运营控制台 |
| 回归测试 | `client/src/lib/*.test.ts` | 用明确数值基线锁定各层行为 |

## 模型与边界

评审时应优先确认边界文案是否伴随用户可见的结果。RC 使用单支路理想解析式及数值近似；LED 使用固定正向压降或明确的教学 I–V 近似；高级器件和运放/555/逻辑模块均使用有限输入范围的确定性教学模型；SPICE 仅解析受限的 V/R/C/D 子集。代码与界面都不应使用户将这些近似解释为真实器件数据、认证结论或实际硬件安全批准。

> **评审建议**：重点检查“数字从哪里来”“何时失效”“是否有测试固定其行为”，而不仅是视觉界面和功能入口数量。

## 建议的评审流程

首先执行 `pnpm check && pnpm test && pnpm build`，确认可复现基线。随后依次打开四个主入口，修改至少一个参数、运行分析或触发故障，并检查画布、读数、图表、诊断、导出内容是否使用同一份状态。最后查看 `client/src/lib` 中各分析函数与同名测试文件，确认关键公式、异常路径和交付边界能够被独立审计。

## 已知后续边界

该项目还没有完整 MNA 多节点求解、真实器件厂商模型、完整 SPICE 方言、云端协作、多人冲突处理、真实硬件数据采集或生产认证流程。这些能力的加入必须保留当前的版本化项目模型、数值回归基线、错误诊断和教学边界表达。
