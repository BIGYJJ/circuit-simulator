# RC 充电实验契约

本增量实现一个可解释、可验证的**理想串联 RC 充电实验**。实验由 5 V 直流源、闭合开关、10 kΩ 电阻、100 μF 电容和参考地构成。开关在 `t = 0` 闭合，电容初始电压为 0 V；输出只测量电容上端相对于 GND 的电压 `Vcap`。模型刻意排除电容 ESR、漏电、开关电阻、负载、电源内阻、非理想初始条件和放电过程，界面会将这些限制呈现为教学模型假设。

| 量 | 首期定义 | 默认值 | 验收基线 |
| --- | --- | ---: | --- |
| 源电压 `Vs` | 施加在串联支路的理想 DC 电压 | 5 V | `Vcap(∞) = 5 V` |
| 电阻 `R` | 限制充电电流的理想电阻 | 10 kΩ | `I(0) = Vs / R = 0.5 mA` |
| 电容 `C` | 初始电压为 0 的理想电容 | 100 μF | `τ = R × C = 1.0 s` |
| 时间常数 `τ` | 电压达到最终值约 63.2% 的特征时间 | 1.0 s | `Vcap(τ) ≈ 3.1606 V` |
| 求解时间窗 | 从 0 至 `5τ` 的等间隔采样 | 5.0 s | `Vcap(5τ) ≈ 4.9663 V` |
| 显示采样 | 501 个等间隔点，浏览器本地预计算 | 10 ms/点 | 曲线、游标和画布读数使用同一时间点 |

## 计算与显示边界

瞬态核心会将每个采样点计算为 `Vcap(t) = Vs × (1 − e^(−t/RC))`，同时给出支路电流 `I(t) = Vs/R × e^(−t/RC)`。这使第一版可以用解析式建立明确的黄金基线；后续 RC 网络、开关事件与非线性器件将迁移至统一的数值积分/MNA 后端，但必须保留上述结果作为回归对照。

播放状态只是移动时间游标，不重新执行物理计算。点击播放后，游标在预计算样本上以真实时间向前推进；暂停保持当前快照；单步前进一个样本；复位将游标和开关状态恢复到 `t = 0`。若用户更改 R、C 或 Vs 并应用参数，旧结果与游标立即失效，只有重新运行实验才产生新的曲线。

## 首期界面映射

中心画布采用参考图中 RC 实验的紧凑串联结构，并在充电时以青蓝测量点和萤光石灰方向指示表现当前状态。底部测量台升级为示波器，曲线保持青蓝，时间常数游标保持萤光石灰。右侧检视器在选择 C1 时显示电容、初始电压和模型公式，并用解释卡说明“在 `1τ` 时已充至约 63%”。工作台不将视觉动画或简化模型描述为对实际硬件行为的保证。
